import { createMuiTheme} from '@material-ui/core/styles';

const theme = {
    palette: {
        primary: {
            main: "rgb(97, 205, 187)",
        },
        secondary: {
            main: "rgb(244,117,96)",
        },
    },
};

export default createMuiTheme(theme);